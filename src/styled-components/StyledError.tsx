import styled from 'styled-components';

export const StyledError = styled.span`
	color: #b71614;
	font-size: 0.75rem;
	margin-left: 0.8rem;
	display: inline-flex;
	align-items: center;
	column-gap: 0.4ch;
`;
